# Too Long; Didn't Watch

A Streamlit app that extracts YouTube video transcripts (via SerpAPI) and lets you chat with them using RAG. Supports BM25 keyword retrieval or semantic search (BAAI/bge-small-en-v1.5 + FAISS). LLM calls go through litellm/OpenRouter.
