can you merge in all of the worktrees in the following directory:

$ARGUMENTS 

fix merge conflicts if any

and also prompt the user at the end for the relavent worktrees' branch removal (.e.g `git branch -d <branch>`)
